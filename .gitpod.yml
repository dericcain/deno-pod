image:
  file: .gitpod.Dockerfile

github:
  prebuilds:
    main: true
    branches: true
    pullRequests: true
    addCheck: false
    addComment: false
    addBadge: true

ports:
  - name: web
    port: 1993
    visibility: public
    onOpen: open-preview

jetbrains:
  webstorm:
    prebuilds:
      version: stable
    plugins:
      - izhangzhihao.rainbow.brackets
      
tasks:
  - name: Fresh
    command: deno task start --watch
      
